# Phase 2 — worktrees des agents (gérés par git worktree)
.swarm/

# Clone temporaire pi-mono (vérification compatibilité swarm)
.pi-mono-temp/

# Env / secrets
.env
.env.local
*.pem

# OS / éditeurs
.DS_Store
.idea/
*.swp
*.swo
